# Copy to .env.local
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
# Set to false to temporarily disable guest lock UI/routing during testing.
# true = lock guest features, false = unlock for all visitors
NEXT_PUBLIC_GUEST_LOCK_ENABLED=true
